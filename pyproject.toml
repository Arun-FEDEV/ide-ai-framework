[project]
name = "ide-ai-framework"
version = "0.1.0"
description = "A Codex-style control plane for VS Code GitHub Copilot Chat."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "IDE AI Framework contributors" }]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
